<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AETHELGARD | The Ledger is Initializing</title>
    <style>
        body {
            background-color: #050505;
            color: #d4af37; /* Aethelgard Gold */
            font-family: 'Courier New', Courier, monospace;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            overflow: hidden;
            text-align: center;
        }

        .logo-circle {
            width: 150px;
            height: 150px;
            border: 2px solid #d4af37;
            border-radius: 50%;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 0 20px rgba(212, 175, 55, 0.2);
            animation: pulse 3s infinite ease-in-out;
        }

        @keyframes pulse {
            0% { transform: scale(1); opacity: 0.8; }
            50% { transform: scale(1.05); opacity: 1; box-shadow: 0 0 40px rgba(212, 175, 55, 0.4); }
            100% { transform: scale(1); opacity: 0.8; }
        }

        h1 {
            letter-spacing: 10px;
            font-weight: 300;
            margin: 10px 0;
            text-transform: uppercase;
        }

        p {
            color: #888;
            max-width: 500px;
            line-height: 1.6;
        }

        .countdown {
            font-size: 2rem;
            margin: 30px 0;
            letter-spacing: 5px;
            color: #fff;
        }

        .btn-initialize {
            background: transparent;
            border: 1px solid #d4af37;
            color: #d4af37;
            padding: 15px 40px;
            text-transform: uppercase;
            letter-spacing: 2px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .btn-initialize:hover {
            background: #d4af37;
            color: #050505;
            box-shadow: 0 0 30px rgba(212, 175, 55, 0.5);
        }

        .footer {
            position: absolute;
            bottom: 20px;
            font-size: 0.7rem;
            color: #444;
            letter-spacing: 1px;
        }
    </style>
</head>
<body>

    <div class="logo-circle">
        <span style="font-size: 0.8rem;">THE LEDGER</span>
    </div>

    <h1>AETHELGARD</h1>
    <p>The 404 is over. The old world is closing. The Collective Labor Ledger is initializing for the 2026 Genesis Launch.</p>

    <div class="countdown" id="timer">00:00:00:00</div>

    <a href="https://github.com/aethelgard" class="btn-initialize">Join the Vested</a>

    <div class="footer">
        PROTOCOL: ARTICLE IV SECURED | CIRCLE OF FIVE VERIFIED | © 2026
    </div>

    <script>
        // Set the date we're counting down to (Midnight Jan 1, 2026)
        var countDownDate = new Date("Jan 1, 2026 00:00:00").getTime();

        var x = setInterval(function() {
            var now = new Date().getTime();
            var distance = countDownDate - now;

            var hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            var minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            var seconds = Math.floor((distance % (1000 * 60)) / 1000);

            document.getElementById("timer").innerHTML = 
                (hours < 10 ? "0" + hours : hours) + ":" + 
                (minutes < 10 ? "0" + minutes : minutes) + ":" + 
                (seconds < 10 ? "0" + seconds : seconds);

            if (distance < 0) {
                clearInterval(x);
                document.getElementById("timer").innerHTML = "LEDGER OPEN";
            }
        }, 1000);
    </script>
</body>
</html>
